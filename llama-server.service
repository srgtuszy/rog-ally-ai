[Unit]
Description=llama.cpp Inference Server
After=network.target

[Service]
Type=simple
ExecStart=%h/gpu-setup/stack.sh daemon
Restart=on-failure
RestartSec=5

Environment=HOME=%h
EnvironmentFile=%h/.config/gpu-setup/gpu-setup.env

[Install]
WantedBy=default.target
